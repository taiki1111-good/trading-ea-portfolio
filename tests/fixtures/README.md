# Test Fixtures

All `spread` values in the CSV fixtures are expressed in pips.

- `0.2` means 0.2 pips.
- `0.3` means 0.3 pips.

This is consistent with the Data layer contract that reconstructs bid/ask values from `close` and pip-based `spread`.
